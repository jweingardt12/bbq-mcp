# Smithery Configuration for BBQ MCP Server
# https://smithery.ai/docs/build/project-config

# TypeScript runtime - Smithery CLI handles the build
# Entry point is defined in package.json "module" field (src/smithery.ts)
# configSchema is exported from the TypeScript entry point
runtime: typescript
