# Smithery Configuration for BBQ MCP Server
# https://smithery.ai/docs/build/deployments/typescript

runtime: "typescript"

# Entry point - exports createServer and configSchema
entrypoint: "src/smithery.ts"
